{
  "name": "scanned-pdf",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && tsc -b && vite build",
    "build:wasm": "nix shell nixpkgs#lld -c cargo build -p scan-wasm --release --target wasm32-unknown-unknown && mkdir -p src/wasm && cp target/wasm32-unknown-unknown/release/scan_wasm.wasm src/wasm/scan_wasm.wasm",
    "cli": "cargo run -p scan-cli --",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^5.4.394",
    "vite": "^7.1.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "typescript": "^5.8.0"
  }
}
